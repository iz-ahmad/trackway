# Planning documents. Kept locally, not tracked.
docs/plans/
docs/brainstorms/
engineering-memory-v1-spec.md

# Backstory's own output. It reads this repository's sessions like any other,
# and the records it writes are this project's working notes, not its source.
.backstory/

# Dependencies and build output
node_modules/
dist/
build/

# The explorer bundle is built at publish time, not committed.
!packages/ui/dist/
*.tsbuildinfo
coverage/

# Environment and secrets
.env
.env.*
!.env.example

# Design skill working directory.
.impeccable/

# Editor and OS
.DS_Store
.idea/
.vscode/
*.swp
